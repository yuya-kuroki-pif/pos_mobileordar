{
  "name": "pos-mobileorder",
  "version": "0.2.0",
  "private": true,
  "description": "飲食店向け POS / モバイルオーダー（店舗端末）と管理ダッシュボード",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=dashboard",
    "dev:terminal": "npm run dev --workspace=terminal",
    "build": "npm run build --workspace=dashboard && npm run build --workspace=terminal",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
